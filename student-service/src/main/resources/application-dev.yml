spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/mydb?retryWrites=true&w=majority
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/